{
	"logo": "assets/xrhodes.mtl",
	"assets/xrhodes.tex", # sorted after a dependent; must not be left as external dependency, and must be correctly resolved as internal dependency.
	"mytex": "assets/xrhodes.tex", # this is a duplicate, and shall be ignored, but it defines an alias, which must be used.
	"texture": "assets/xrhodes.tex", # ignore duplicate, override alias.
}